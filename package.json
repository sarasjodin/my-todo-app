{
  "name": "my-todo-app",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "clean": "rmdir /s /q dist",
    "deploy": "npm run clean && npm run build && gh-pages -d dist",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  }
}
